<h1>ログイン</h1>
<p><%= @client[:name] %>にアカウントへのアクセスを許可しますか？</p>

<%= form_tag("/approve", method: :post) do %>
  <%= hidden_field_tag :request_id, @request_id %>

  <div>
    <%= "email"%>
    <%= text_field_tag :email %>
  </div>

  <div>
    <%= "password" %>
    <%= password_field_tag :password %>
  </div>

  <div>
    <p>このアプリケーションは、次の情報にアクセスします</p>
    <ul>
      <% @client[:scope].split(' ').each do |scope| %>
        <li><%= scope %></li>
      <% end %>
    </ul>
  </div>

  <div>
    <%= submit_tag "許可する" %>
  </div>
<% end %>
